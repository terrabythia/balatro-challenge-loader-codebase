import { http, HttpResponse } from "msw";

// No data-fetching components yet — handlers remain minimal.
// Add endpoints here as components that call external APIs are created.
export const mswHandlers = {};
